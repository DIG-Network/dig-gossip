# STR-002: Module Hierarchy

> **Authoritative requirement:** [NORMATIVE.md](../NORMATIVE.md)
> **Verification:** [VERIFICATION.md](../VERIFICATION.md)
> **Tracking:** [TRACKING.yaml](../TRACKING.yaml)
> **Spec reference:** [SPEC.md - Section 10.1](../../../resources/SPEC.md)

## Summary

The crate's module hierarchy (directory and file layout under `src/`) must match the structure defined in SPEC Section 10.1. This ensures consistent code organization, correct module visibility, and predictable file locations for all components of the gossip layer.

## Specification

The `src/` directory MUST contain the following modules and files:

```
src/
├── lib.rs                         # Crate root: re-exports from chia crates + DIG types
│
├── types/
│   ├── mod.rs                     # Re-exports
│   ├── peer.rs                    # PeerId (alias), PeerInfo, PeerConnection
│   ├── config.rs                  # GossipConfig, IntroducerConfig, RelayConfig
│   ├── stats.rs                   # GossipStats, RelayStats
│   ├── reputation.rs              # PeerReputation, PenaltyReason
│   └── dig_messages.rs            # DigMessageType enum (200+ range)
│
├── constants.rs                   # DIG constants + ported Chia Python constants
├── error.rs                       # GossipError (wraps ClientError)
│
├── service/
│   ├── mod.rs
│   ├── gossip_service.rs          # GossipService (construction, start/stop)
│   └── gossip_handle.rs           # GossipHandle (broadcast, send_to, request, stats)
│
├── connection/
│   ├── mod.rs
│   └── listener.rs                # TcpListener + TLS accept
│
├── discovery/
│   ├── mod.rs
│   ├── address_manager.rs         # Rust port of address_manager.py
│   ├── address_manager_store.rs   # Persistent serialization for address manager
│   ├── node_discovery.rs          # Rust port of node_discovery.py discovery loop
│   ├── introducer_client.rs       # Introducer query + registration
│   └── introducer_peers.rs        # VettedPeer, IntroducerPeers
│
├── relay/
│   ├── mod.rs
│   ├── relay_client.rs            # Relay WebSocket client
│   ├── relay_service.rs           # Relay lifecycle with auto-reconnect
│   └── relay_types.rs             # RelayMessage, RelayPeerInfo, RelayConfig, RelayError
│
├── gossip/
│   ├── mod.rs
│   ├── plumtree.rs                # Plumtree eager/lazy push state machine
│   ├── compact_block.rs           # Compact block encoding/decoding/reconstruction
│   ├── erlay.rs                   # ERLAY flood set + minisketch reconciliation
│   ├── priority.rs                # MessagePriority, PriorityOutbound queue
│   ├── backpressure.rs            # Adaptive backpressure monitor
│   ├── broadcaster.rs             # Top-level broadcast orchestration
│   ├── seen_set.rs                # LRU message deduplication
│   └── message_cache.rs           # LRU message cache for GRAFT responses
│
└── util/
    ├── mod.rs
    ├── ip_address.rs              # get_group(), get_key() for PeerInfo bucketing
    ├── as_lookup.rs               # AS number lookup from cached BGP prefix table
    └── latency.rs                 # RTT tracker, peer scoring
```

Each module directory MUST contain a `mod.rs` that declares and re-exports its submodules.

### Module Responsibilities

| Module | Responsibility |
|--------|---------------|
| `types/` | Data types shared across the crate (peer, config, stats, reputation, DIG messages) |
| `constants.rs` | DIG-specific constants and ported Chia Python constants |
| `error.rs` | Unified error type wrapping ClientError with gossip-specific variants |
| `service/` | GossipService construction/lifecycle and GossipHandle for runtime interaction |
| `connection/` | Inbound TCP/TLS listener (outbound drives the handshake itself, then upgrades to DigLink) |
| `discovery/` | Address manager, node discovery loop, introducer client, vetted peers |
| `relay/` | Relay WebSocket client, relay service lifecycle, relay-specific types |
| `gossip/` | Plumtree, compact blocks, ERLAY, priority lanes, backpressure, dedup |
| `util/` | IP address helpers, AS lookup, latency tracking |

## Acceptance Criteria

- [ ] `src/lib.rs` exists and is the crate root
- [ ] `src/types/mod.rs` exists and declares submodules: `peer`, `config`, `stats`, `reputation`, `dig_messages`
- [ ] `src/types/peer.rs` exists
- [ ] `src/types/config.rs` exists
- [ ] `src/types/stats.rs` exists
- [ ] `src/types/reputation.rs` exists
- [ ] `src/types/dig_messages.rs` exists
- [ ] `src/constants.rs` exists
- [ ] `src/error.rs` exists
- [ ] `src/service/mod.rs` exists and declares submodules: `gossip_service`, `gossip_handle`
- [ ] `src/service/gossip_service.rs` exists
- [ ] `src/service/gossip_handle.rs` exists
- [ ] `src/connection/mod.rs` exists and declares submodule: `listener`
- [ ] `src/connection/listener.rs` exists
- [ ] `src/discovery/mod.rs` exists and declares submodules: `address_manager`, `address_manager_store`, `node_discovery`, `introducer_client`, `introducer_peers`
- [ ] All `src/discovery/*.rs` files exist
- [ ] `src/relay/mod.rs` exists and declares submodules: `relay_client`, `relay_service`, `relay_types`
- [ ] All `src/relay/*.rs` files exist
- [ ] `src/gossip/mod.rs` exists and declares submodules: `plumtree`, `compact_block`, `erlay`, `priority`, `backpressure`, `broadcaster`, `seen_set`, `message_cache`
- [ ] All `src/gossip/*.rs` files exist
- [ ] `src/util/mod.rs` exists and declares submodules: `ip_address`, `as_lookup`, `latency`
- [ ] All `src/util/*.rs` files exist
- [ ] `cargo check` succeeds with no missing module errors

## Implementation Notes

- Primary file: All files under `src/`
- Dependencies: None (this is structural)
- Edge cases:
  - `relay/` module content should be conditionally compiled with `#[cfg(feature = "relay")]`
  - `gossip/erlay.rs` should be conditionally compiled with `#[cfg(feature = "erlay")]`
  - `gossip/compact_block.rs` should be conditionally compiled with `#[cfg(feature = "compact-blocks")]`
  - Each `mod.rs` must use `pub mod` for submodules that need to be accessible from other modules

## Verification

### Test Plan

| Test | Type | Description | Expected Result |
|------|------|-------------|-----------------|
| test_lib_rs_exists | Unit | Verify src/lib.rs exists | File exists |
| test_types_module_structure | Unit | Verify all files in types/ exist | mod.rs, peer.rs, config.rs, stats.rs, reputation.rs, dig_messages.rs all exist |
| test_constants_module_exists | Unit | Verify src/constants.rs exists | File exists |
| test_error_module_exists | Unit | Verify src/error.rs exists | File exists |
| test_service_module_structure | Unit | Verify all files in service/ exist | mod.rs, gossip_service.rs, gossip_handle.rs all exist |
| test_connection_module_structure | Unit | Verify all files in connection/ exist | mod.rs, listener.rs all exist |
| test_discovery_module_structure | Unit | Verify all files in discovery/ exist | mod.rs, address_manager.rs, address_manager_store.rs, node_discovery.rs, introducer_client.rs, introducer_peers.rs all exist |
| test_relay_module_structure | Unit | Verify all files in relay/ exist | mod.rs, relay_client.rs, relay_service.rs, relay_types.rs all exist |
| test_gossip_module_structure | Unit | Verify all files in gossip/ exist | mod.rs, plumtree.rs, compact_block.rs, erlay.rs, priority.rs, backpressure.rs, broadcaster.rs, seen_set.rs, message_cache.rs all exist |
| test_util_module_structure | Unit | Verify all files in util/ exist | mod.rs, ip_address.rs, as_lookup.rs, latency.rs all exist |
| test_module_compilation | Integration | Run cargo check to verify module tree compiles | Compilation succeeds |

### Expected Test Files

- tests/str_002_tests.rs
